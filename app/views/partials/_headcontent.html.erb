<%= render "partials/headnav" %>
<!-- #wrapper -->
<div id="wrapper">
  <%= render "partials/sidenav" %>
  <!-- #content-wrapper -->
  <div id="content-wrapper">
    <%= render "partials/flashcontent" %>
    <!-- .container-fluid -->
    <div class="container-fluid">
      <%= render "partials/breadcrumbs" %>